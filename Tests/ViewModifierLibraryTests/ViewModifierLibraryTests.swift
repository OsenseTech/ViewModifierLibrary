import XCTest
@testable import ViewModifierLibrary

final class ViewModifierLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ViewModifierLibrary().text, "Hello, World!")
    }
}
